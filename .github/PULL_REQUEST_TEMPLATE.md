# Summary

<!-- What does this PR change, and why? Link the issue if there is one. -->

## How was this tested?

<!-- Penlight has no automated UI tests — describe the manual scenarios you ran
     on Windows (which modes, how many monitors, DPI scaling, OBS capture if
     relevant). -->

## Checklist

- [ ] `npm run build` passes (tsc + vite)
- [ ] `cargo check --locked` passes in `src-tauri/`
- [ ] Verified the affected feature(s) by running the app
- [ ] README / CHANGELOG updated if this is user-facing
