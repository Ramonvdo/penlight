{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal capability: all privileged work happens in Rust commands",
  "windows": ["overlay-*", "toolbar", "settings"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
